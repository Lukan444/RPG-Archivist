/**
 * Test tokens for authentication tests
 * These are mock values used only for testing purposes
 */
export const TEST_TOKENS = {
  VALID_TOKEN: 'TEST_MOCK_TOKEN',
  INVALID_TOKEN: 'TEST_INVALID_TOKEN'
};
