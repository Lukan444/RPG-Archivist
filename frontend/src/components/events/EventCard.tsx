import React from 'react';
import { Box, Typography, Card, CardContent } from '@mui/material';

const EventCard: React.FC = () => {
  return (
    <Card>
      <CardContent>
        <Typography variant="h6">Event Card</Typography>
        <Typography variant="body2">This component is under development.</Typography>
      </CardContent>
    </Card>
  );
};

export default EventCard;
